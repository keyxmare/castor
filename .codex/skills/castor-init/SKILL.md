---
name: castor-init
description: Transforme un clone frais de Castor en nouveau projet (renommage, home de démo retirée, historique git neuf). Destructif, à lancer une seule fois.
---

# castor-init (Codex)

Source unique de la procédure : `.claude/skills/castor-init/SKILL.md`.

Avant d'agir, lis `AGENTS.md` (standards d'équipe → `.claude/CLAUDE.md`), puis ouvre et suis intégralement `.claude/skills/castor-init/SKILL.md`.

Frontière de portabilité (cf. AGENTS.md) : pas de subagents ni de hooks sous Codex → audits en passe inline, `make check-fast` avant tout commit, jamais de commit sur `main`.
